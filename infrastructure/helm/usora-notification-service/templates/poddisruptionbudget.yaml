{{- if gt (int (.Values.autoscaling.minReplicas | default .Values.replicaCount)) 1 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "usora-notification-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "usora-notification-service.labels" . | nindent 4 }}
spec:
  minAvailable: 50%
  selector:
    matchLabels:
      {{- include "usora-notification-service.selectorLabels" . | nindent 6 }}
{{- end }}
