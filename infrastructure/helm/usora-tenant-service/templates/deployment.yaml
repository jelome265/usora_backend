apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "usora-tenant-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "usora-tenant-service.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "usora-tenant-service.selectorLabels" . | nindent 6 }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        {{- include "usora-tenant-service.labels" . | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "usora-tenant-service.serviceAccountName" . }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: usora-tenant-service
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          ports:
            - name: http
              containerPort: 8083
              protocol: TCP
            - name: grpc
              containerPort: 9090
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "usora-tenant-service.fullname" . }}-config
          env:
            {{- if .Values.postgresql.runtimeRole.existingSecret }}
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.postgresql.runtimeRole.existingSecret }}
                  key: {{ .Values.postgresql.runtimeRole.existingSecretUsernameKey | default "username" }}
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.postgresql.runtimeRole.existingSecret }}
                  key: {{ .Values.postgresql.runtimeRole.existingSecretPasswordKey | default "password" }}
            {{- else }}
            {{ required "postgresql.runtimeRole.existingSecret must be set — this is the restricted role RLS depends on (see V3__row_level_security.sql)." "" }}
            {{- end }}
            {{- if .Values.postgresql.migrationRole.existingSecret }}
            - name: SPRING_FLYWAY_USER
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.postgresql.migrationRole.existingSecret }}
                  key: {{ .Values.postgresql.migrationRole.existingSecretUsernameKey | default "username" }}
            - name: SPRING_FLYWAY_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.postgresql.migrationRole.existingSecret }}
                  key: {{ .Values.postgresql.migrationRole.existingSecretPasswordKey | default "password" }}
            {{- else }}
            {{ required "postgresql.migrationRole.existingSecret must be set — without it Flyway reuses the restricted runtime role and every DDL statement in V3__row_level_security.sql fails." "" }}
            {{- end }}
            {{- if .Values.redis.existingSecret }}
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.redis.existingSecret }}
                  key: {{ .Values.redis.existingSecretPasswordKey | default "password" }}
            {{- else }}
            {{ required "redis.existingSecret must be set — REDIS_PASSWORD has no default in application-prod.yml." "" }}
            {{- end }}
            {{- if .Values.secrets.existingSecret }}
            - name: STRIPE_WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.existingSecret }}
                  key: stripe-webhook-secret
                  optional: true
            {{- end }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          {{- with .Values.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      volumes:
        - name: tmp
          emptyDir: {}
